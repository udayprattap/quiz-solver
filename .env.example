# Example .env file for TDS Quiz Solver
# Copy this to .env and fill in your actual values

# Your email address for quiz submissions
EMAIL=your.email@example.com

# Your secret key for authentication
SECRET=your_secret_key

# Optional: AI API token from IITM for LLM-powered question analysis
# When configured, the solver uses ChatGPT/GPT-4 for intelligent question understanding
# Falls back to rule-based logic if PIPE_TOKEN not set or API fails
# Get your token from: IITM student portal
# Keep this secure - do NOT commit to GitHub
PIPE_TOKEN=your_pipe_token_here

# Optional: Server port (default: 7860 for Render.com/HF Spaces)
PORT=7860

# Optional: Keep service awake with self-ping (1=enabled, 0=disabled)
ENABLE_KEEP_ALIVE=1

# Optional: Disable Playwright browser automation (1=disabled, 0=enabled)
DISABLE_PLAYWRIGHT=0
