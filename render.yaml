services:
  - type: web
    name: tds-quiz-solver
    env: docker
    plan: free
    autoDeploy: true
    region: singapore
    dockerfilePath: Dockerfile
    healthCheckPath: /
    envVars:
      - key: EMAIL
        value: 24ds3000019@ds.study.iitm.ac.in
      - key: SECRET
        value: banana
      - key: PIPE_TOKEN
        sync: false  # Will be added as secret via dashboard
      - key: PORT
        value: 7860
